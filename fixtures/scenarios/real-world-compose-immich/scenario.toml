# Pinned upstream source with independently transcribed intent and exact route evidence.
schema = 1
id = "real-world-compose-immich"
source-capabilities = ["compose"]
target-capabilities = ["compose", "podman", "quadlet"]
protected-values = []

[application]
name = "immich"
revision = "1c7c28bb0d5c4a2edba062806efff50921b489b2"

[[components]]
name = "immich-server"
configured-image = "ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}"

[[components]]
name = "immich-machine-learning"
configured-image = "ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}"

[[components]]
name = "redis"
configured-image = "docker.io/valkey/valkey:9@sha256:3acc0687f2a2e1091fae6450d7842dd658c941338cf0a873ddd9e14b9e4ea4dd"

[[components]]
name = "database"
configured-image = "ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23"

[[native-inputs]]
id = "compose"
importer = "compose"
files = ["input.compose.yaml"]
format-version = "compose-specification"
all-profiles = true
import-diagnostics-file = "expected.import.diagnostics"

[provenance]
source = "external"
corpus-project = "immich"
license = "AGPL-3.0-only"
privacy-reviewed = true
test-data = "Immutable public upstream Compose blob; no environment files, credentials, or runtime observations were added."
image-evidence = "Configured image/build references are independently transcribed; no image is pulled."

[deployment]
origin = "external"
root-mode = "not-applicable"

[semantics]
selected-resources = [
  "service:immich-server",
  "service:immich-machine-learning",
  "service:redis",
  "service:database",
  "volume:model-cache",
]
excluded-resources = []
ownership-boundaries = [
  "volume:model-cache:application",
]
shared-boundaries = [
  "volume:model-cache:consumers=immich-machine-learning",
]
required-mounts = [
  "immich-machine-learning:model-cache:/cache",
]
# REVIEW-GAP unrepresentable relative/plain bind: immich-server:${UPLOAD_LOCATION}:/data:read-only=false:selinux=none
# REVIEW-GAP unrepresentable relative/plain bind: immich-server:/etc/localtime:/etc/localtime:read-only=true:selinux=none
# REVIEW-GAP unrepresentable relative/plain bind: database:${DB_DATA_LOCATION}:/var/lib/postgresql/data:read-only=false:selinux=none
required-environment = [
  "database:POSTGRES_INITDB_ARGS=--data-checksums",
]
required-environment-order = []
required-restart = [
  "immich-server:always",
  "immich-machine-learning:always",
  "redis:always",
  "database:always",
]
required-runtime-names = [
  "immich-server:immich_server",
  "immich-machine-learning:immich_machine_learning",
  "redis:immich_redis",
  "database:immich_postgres",
]
unpublished-services = [
  "immich-machine-learning",
  "redis",
  "database",
]
external-prerequisites = []
expected-diagnostics = []
required-image-builds = []
required-image-sources = [
  { service = "immich-server", kind = "literal", value = "ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}" },
  { service = "immich-machine-learning", kind = "literal", value = "ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}" },
  { service = "redis", kind = "literal", value = "docker.io/valkey/valkey:9@sha256:3acc0687f2a2e1091fae6450d7842dd658c941338cf0a873ddd9e14b9e4ea4dd" },
  { service = "database", kind = "literal", value = "ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23" },
]
required-commands = [
  { service = "immich-server", kind = "absent", values = [] },
  { service = "immich-machine-learning", kind = "absent", values = [] },
  { service = "redis", kind = "absent", values = [] },
  { service = "database", kind = "absent", values = [] },
]
required-entrypoints = [
  { service = "immich-server", kind = "absent", values = [] },
  { service = "immich-machine-learning", kind = "absent", values = [] },
  { service = "redis", kind = "absent", values = [] },
  { service = "database", kind = "absent", values = [] },
]
required-dependencies = [
  { service = "immich-server", dependency = "redis", condition = "unspecified" },
  { service = "immich-server", dependency = "database", condition = "unspecified" },
]
required-healthchecks = [
  { service = "redis", command-kind = "shell", command = ["redis-cli ping || exit 1"] },
]
required-port-bindings = [
  { service = "immich-server", container = 2283, published = 2283, protocol = "tcp" },
]
required-volume-mounts = [
  { service = "immich-machine-learning", source = "model-cache", target = "/cache", read-only = false },
]
required-networks = []

[[evidence]]
input = "compose"
exporter = "compose"
target-implementation = "compose-specification"
target-minimum = "1.0.0"
target-maximum = "1.0.0"
loss-policy = "partial"
outcome = "known-migration-gap"
reason = "Compose output is emitted, but reimport cannot reconstruct the reviewed dependencies and healthcheck."
artifacts-file = "expected.compose.artifacts"
diagnostics-file = "expected.compose.diagnostics"
reimport-diagnostics-file = "expected.compose.reimport.diagnostics"
allowed-losses-file = "expected.compose.losses.tsv"
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "known-migration-gap", reason = "Generated Compose cannot reconstruct the reviewed dependencies and healthcheck." }
reimport-semantic-gaps = ["dependency:immich-server:database", "dependency:immich-server:redis", "healthcheck:redis"]

[[evidence]]
input = "compose"
exporter = "quadlet"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "partial"
outcome = "expected-rejection"
reason = "Rejected before artifact emission; no native artifact was emitted."
artifacts = []
diagnostics-file = "expected.quadlet.diagnostics"
allowed-losses-file = "expected.quadlet.losses.tsv"
native-validation = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }

[[evidence]]
input = "compose"
exporter = "podman"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "partial"
outcome = "expected-rejection"
reason = "Rejected before artifact emission; no native artifact was emitted."
artifacts = []
diagnostics-file = "expected.podman.diagnostics"
allowed-losses-file = "expected.podman.losses.tsv"
native-validation = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
