# Pinned upstream source with independently transcribed intent and exact route evidence.
schema = 1
id = "real-world-compose-nextcloud"
source-capabilities = ["compose"]
target-capabilities = ["compose", "podman", "quadlet"]
protected-values = []

[application]
name = "nextcloud"
revision = "4de1eed65a44c61ecce601ef1d27abd2059b52e6"

[[components]]
name = "db"
configured-image = "postgres:alpine"

[[components]]
name = "redis"
configured-image = "redis:alpine"

[[components]]
name = "app"
configured-image = "nextcloud:fpm-alpine"

[[components]]
name = "web"
configured-image = "nginx:alpine-slim"

[[components]]
name = "cron"
configured-image = "nextcloud:fpm-alpine"

[[components]]
name = "letsencrypt-companion"
configured-image = "nginxproxy/acme-companion"

[[native-inputs]]
id = "compose"
importer = "compose"
files = ["input.compose.yaml"]
format-version = "compose-specification"
all-profiles = true

[provenance]
source = "external"
corpus-project = "nextcloud"
license = "AGPL-3.0-only"
privacy-reviewed = true
test-data = "Immutable public upstream Compose blob; no environment files, credentials, or runtime observations were added."
image-evidence = "Configured image/build references are independently transcribed; no image is pulled."

[deployment]
origin = "external"
root-mode = "not-applicable"

[semantics]
selected-resources = [
  "service:db",
  "service:redis",
  "service:app",
  "service:web",
  "service:cron",
  "service:proxy",
  "service:letsencrypt-companion",
  "volume:db",
  "volume:nextcloud",
  "volume:certs",
  "volume:acme",
  "volume:vhost.d",
  "volume:html",
  "network:proxy-tier",
  "image-build:proxy-build",
]
excluded-resources = []
ownership-boundaries = [
  "volume:db:application",
  "volume:nextcloud:application",
  "volume:certs:application",
  "volume:acme:application",
  "volume:vhost.d:application",
  "volume:html:application",
]
shared-boundaries = [
  "volume:db:consumers=db",
  "volume:nextcloud:consumers=app,web,cron",
  "volume:certs:consumers=proxy,letsencrypt-companion",
  "volume:acme:consumers=letsencrypt-companion",
  "volume:vhost.d:consumers=proxy,letsencrypt-companion",
  "volume:html:consumers=proxy,letsencrypt-companion",
]
required-mounts = [
  "db:db:/var/lib/postgresql",
  "app:nextcloud:/var/www/html",
  "web:nextcloud:/var/www/html",
  "cron:nextcloud:/var/www/html",
  "proxy:certs:/etc/nginx/certs",
  "proxy:vhost.d:/etc/nginx/vhost.d",
  "proxy:html:/usr/share/nginx/html",
  "letsencrypt-companion:certs:/etc/nginx/certs",
  "letsencrypt-companion:acme:/etc/acme.sh",
  "letsencrypt-companion:vhost.d:/etc/nginx/vhost.d",
  "letsencrypt-companion:html:/usr/share/nginx/html",
]
# REVIEW-GAP unrepresentable relative/plain bind: web:./web/nginx.conf:/etc/nginx/nginx.conf:read-only=true:selinux=none
# REVIEW-GAP unrepresentable relative/plain bind: proxy:/var/run/docker.sock:/tmp/docker.sock:read-only=true:selinux=shared
# REVIEW-GAP unrepresentable relative/plain bind: letsencrypt-companion:/var/run/docker.sock:/var/run/docker.sock:read-only=true:selinux=shared
required-environment = [
  "app:POSTGRES_HOST=db",
  "app:REDIS_HOST=redis",
  "web:VIRTUAL_HOST=",
  "web:LETSENCRYPT_HOST=",
  "web:LETSENCRYPT_EMAIL=",
  "letsencrypt-companion:DEFAULT_EMAIL=",
]
required-environment-order = []
required-restart = [
  "db:always",
  "redis:always",
  "app:always",
  "web:always",
  "cron:always",
  "proxy:always",
  "letsencrypt-companion:always",
]
required-runtime-names = []
unpublished-services = [
  "db",
  "redis",
  "app",
  "web",
  "cron",
  "letsencrypt-companion",
]
external-prerequisites = []
expected-diagnostics = []
required-image-builds = [
  "proxy-build",
]
required-image-sources = [
  { service = "db", kind = "literal", value = "postgres:alpine" },
  { service = "redis", kind = "literal", value = "redis:alpine" },
  { service = "app", kind = "literal", value = "nextcloud:fpm-alpine" },
  { service = "web", kind = "literal", value = "nginx:alpine-slim" },
  { service = "cron", kind = "literal", value = "nextcloud:fpm-alpine" },
  { service = "proxy", kind = "build", value = "proxy-build" },
  { service = "letsencrypt-companion", kind = "literal", value = "nginxproxy/acme-companion" },
]
required-commands = [
  { service = "db", kind = "absent", values = [] },
  { service = "redis", kind = "absent", values = [] },
  { service = "app", kind = "absent", values = [] },
  { service = "web", kind = "absent", values = [] },
  { service = "cron", kind = "absent", values = [] },
  { service = "proxy", kind = "absent", values = [] },
  { service = "letsencrypt-companion", kind = "absent", values = [] },
]
required-entrypoints = [
  { service = "db", kind = "absent", values = [] },
  { service = "redis", kind = "absent", values = [] },
  { service = "app", kind = "absent", values = [] },
  { service = "web", kind = "absent", values = [] },
  { service = "cron", kind = "shell", values = ["/cron.sh"] },
  { service = "proxy", kind = "absent", values = [] },
  { service = "letsencrypt-companion", kind = "absent", values = [] },
]
required-dependencies = [
  { service = "app", dependency = "db", condition = "unspecified" },
  { service = "app", dependency = "redis", condition = "unspecified" },
  { service = "app", dependency = "proxy", condition = "unspecified" },
  { service = "web", dependency = "app", condition = "unspecified" },
  { service = "cron", dependency = "db", condition = "unspecified" },
  { service = "cron", dependency = "redis", condition = "unspecified" },
  { service = "letsencrypt-companion", dependency = "proxy", condition = "unspecified" },
]
required-healthchecks = []
required-port-bindings = [
  { service = "proxy", container = 80, published = 80, protocol = "tcp" },
  { service = "proxy", container = 443, published = 443, protocol = "tcp" },
]
required-volume-mounts = [
  { service = "db", source = "db", target = "/var/lib/postgresql", read-only = false, selinux-relabel = "private" },
  { service = "app", source = "nextcloud", target = "/var/www/html", read-only = false, selinux-relabel = "shared" },
  { service = "web", source = "nextcloud", target = "/var/www/html", read-only = true, selinux-relabel = "shared" },
  { service = "cron", source = "nextcloud", target = "/var/www/html", read-only = false, selinux-relabel = "shared" },
  { service = "proxy", source = "certs", target = "/etc/nginx/certs", read-only = true, selinux-relabel = "shared" },
  { service = "proxy", source = "vhost.d", target = "/etc/nginx/vhost.d", read-only = false, selinux-relabel = "shared" },
  { service = "proxy", source = "html", target = "/usr/share/nginx/html", read-only = false, selinux-relabel = "shared" },
  { service = "letsencrypt-companion", source = "certs", target = "/etc/nginx/certs", read-only = false, selinux-relabel = "shared" },
  { service = "letsencrypt-companion", source = "acme", target = "/etc/acme.sh", read-only = false, selinux-relabel = "shared" },
  { service = "letsencrypt-companion", source = "vhost.d", target = "/etc/nginx/vhost.d", read-only = false, selinux-relabel = "shared" },
  { service = "letsencrypt-companion", source = "html", target = "/usr/share/nginx/html", read-only = false, selinux-relabel = "shared" },
]
required-networks = [
  { name = "proxy-tier", ownership = "application" },
]

[[evidence]]
input = "compose"
exporter = "compose"
target-implementation = "compose-specification"
target-minimum = "1.0.0"
target-maximum = "1.0.0"
loss-policy = "partial"
outcome = "expected-rejection"
reason = "Rejected before artifact emission; no native artifact was emitted."
artifacts = []
diagnostics-file = "expected.compose.diagnostics"
allowed-losses-file = "expected.compose.losses.tsv"
native-validation = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }

[[evidence]]
input = "compose"
exporter = "quadlet"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "partial"
outcome = "expected-rejection"
reason = "Rejected before artifact emission; no native artifact was emitted."
artifacts = []
diagnostics-file = "expected.quadlet.diagnostics"
allowed-losses-file = "expected.quadlet.losses.tsv"
native-validation = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }

[[evidence]]
input = "compose"
exporter = "podman"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "partial"
outcome = "expected-rejection"
reason = "Rejected before artifact emission; no native artifact was emitted."
artifacts = []
diagnostics-file = "expected.podman.diagnostics"
allowed-losses-file = "expected.podman.losses.tsv"
native-validation = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Rejected before artifact emission; no native artifact was emitted." }
