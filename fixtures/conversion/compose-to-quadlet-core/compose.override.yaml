services:
  web:
    container_name: ferry-web
    image: registry.example:5000/team/web:1.3@sha256:fedcba
    healthcheck:
      interval: 45s
    environment:
      PORT: 9090
      OVERRIDE_ONLY: enabled
    extra_hosts:
      - database=192.0.2.10
    volumes:
      - ./data:/srv/data:Z,ro
