schema = 1
id = "document-route-normal"
source-capabilities = ["compose", "quadlet"]
target-capabilities = ["compose", "podman", "quadlet"]

[application]
name = "route-matrix"
version = "1.0.0"

[[components]]
name = "web"
version = "1.0.0"
image = "example.invalid/web"
digest = "sha256:dddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd"
configured-image = "example.invalid/web:1"

[[native-inputs]]
id = "compose"
importer = "compose"
files = ["compose.yaml"]
format-version = "compose-specification"

[[native-inputs]]
id = "quadlet"
importer = "quadlet"
files = ["web.container"]
format-version = "0.2.3"

[provenance]
source = "authored"
license = "MPL-2.0"
privacy-reviewed = true
test-data = "Repository-authored synthetic names and values; no captured user data or credentials."
image-evidence = "Synthetic example.invalid image identity is never pulled; the digest-shaped value is offline assertion metadata only."

[deployment]
origin = "authored"
root-mode = "not-applicable"

[semantics]
selected-resources = ["service:web"]
excluded-resources = []
ownership-boundaries = []
shared-boundaries = []
required-mounts = []
required-environment = []
required-ports = []
required-runtime-names = ["web:web-runtime"]
required-restart = ["web:never"]
unpublished-services = ["web"]
external-prerequisites = []
expected-diagnostics = []

[[evidence]]
input = "compose"
exporter = "compose"
target-implementation = "compose-specification"
target-minimum = "1.0.0"
target-maximum = "1.0.0"
loss-policy = "exact"
outcome = "migration-success"
artifacts = ["compose.yaml"]
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "passed" }

[[evidence]]
input = "compose"
exporter = "quadlet"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "exact"
outcome = "migration-success"
artifacts = ["web.container"]
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "passed" }

[[evidence]]
input = "compose"
exporter = "podman"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "partial"
outcome = "known-migration-gap"
reason = "Podman deployment names resources from neutral identifiers and cannot retain the authored runtime container name."
artifacts = ["podman-commands.sh", "podman.json"]
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Deployment plans are not observed Podman inventories." }
diagnostics = ["BFP0007|services.web.runtime_name"]
semantic-gaps = ["runtime-name:web"]
allowed-losses = [
  { rule = "BFP0007", subject = "services.web.runtime_name", decision = "unsupported", version-scope = "podman@5.4.0..6.1.0" },
]

[[evidence]]
input = "quadlet"
exporter = "compose"
target-implementation = "compose-specification"
target-minimum = "1.0.0"
target-maximum = "1.0.0"
loss-policy = "exact"
outcome = "migration-success"
artifacts = ["compose.yaml"]
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "passed" }

[[evidence]]
input = "quadlet"
exporter = "quadlet"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "exact"
outcome = "migration-success"
artifacts = ["web.container"]
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "passed" }

[[evidence]]
input = "quadlet"
exporter = "podman"
target-implementation = "podman"
target-minimum = "5.4.0"
target-maximum = "6.1.0"
loss-policy = "partial"
outcome = "known-migration-gap"
reason = "Podman deployment names resources from neutral identifiers and cannot retain the authored runtime container name."
artifacts = ["podman-commands.sh", "podman.json"]
native-validation = { state = "passed" }
runtime-probe = { state = "not-applicable", reason = "Offline authored fixture; no runtime operation is claimed." }
reimport = { state = "not-applicable", reason = "Deployment plans are not observed Podman inventories." }
diagnostics = ["BFP0007|services.web.runtime_name"]
semantic-gaps = ["runtime-name:web"]
allowed-losses = [
  { rule = "BFP0007", subject = "services.web.runtime_name", decision = "unsupported", version-scope = "podman@5.4.0..6.1.0" },
]
