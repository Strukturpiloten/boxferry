schema = 1
id = "podman-inspect-6-1"
suite = "runtime"
description = "Protects the reviewed Podman 6.1 inspect ceiling and tolerant additive fields."
secrets_reviewed = true
files = [
  "containers.json",
  "images.json",
  "networks.json",
  "pods.json",
  "volumes.json",
]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "Sanitized minimal JSON models the Podman 6.1.0 inspect fields reviewed against tagged source; no runtime or credentials are required."

[expectations]
summary = "The current reviewed schema decodes, while an intentionally unmodeled effective configuration field produces a structured unsupported outcome."

[extensions.podman]
version = "6.1.0"
source-review = "Podman v6.0.2 and v6.1.0 libpod/define/container_inspect.go JSON structs are unchanged; v6.1.0 adds only a Go-template helper method"
