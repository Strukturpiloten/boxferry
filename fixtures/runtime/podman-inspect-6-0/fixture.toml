schema = 1
id = "podman-inspect-6-0"
suite = "runtime"
description = "Protects the reviewed current Podman 6.0 inspect ceiling and tolerant additive fields."
secrets_reviewed = true
files = [
  "containers.json",
  "images.json",
  "networks.json",
  "pods.json",
  "volumes.json",
]

[provenance]
source = "authored"
license = "MPL-2.0"
redistribution = "allowed"
modifications = "none"

[environment]
description = "Sanitized minimal JSON models official Podman 6.0.2 inspect fields; no runtime or credentials are required."

[expectations]
summary = "The current reviewed schema decodes, while an intentionally unmodeled effective configuration field produces a structured unsupported outcome."

[extensions.podman]
version = "6.0.2"
source-review = "Podman v6.0.2 libpod/define/container_inspect.go and current inspect manuals"
